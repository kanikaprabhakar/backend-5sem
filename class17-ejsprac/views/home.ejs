<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
  <style>
        body {
            background: #f5f5f5;
            font-family: Arial, sans-serif;
        }
        h1 {
            text-align: center;
            color: #333;
            margin-top: 2rem;
        }
        .products-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 2rem;
            margin: 2rem auto;
            max-width: 1200px;
        }
        .product-card {
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
            padding: 1rem;
            width: 250px;
            display: flex;
            flex-direction: column;
            align-items: center;
            transition: transform 0.2s;
        }
        .product-card:hover {
            transform: scale(1.03);
            box-shadow: 0 4px 16px rgba(0,0,0,0.12);
        }
        .product-card img {
            width: 200px;
            height: 150px;
            object-fit: cover;
            border-radius: 8px;
            margin-bottom: 1rem;
        }
        .product-name {
            font-size: 1.2rem;
            font-weight: bold;
            color: #007bff;
            margin-bottom: 0.5rem;
        }
        .product-price {
            color: #28a745;
            font-weight: bold;
            margin-bottom: 0.5rem;
        }
        .product-description {
            color: #555;
            margin-bottom: 1rem;
            text-align: center;
        }
        .add-to-cart-btn {
            display: inline-block;
            background: #007bff;
            color: #fff;
            border: none;
            padding: 0.7rem 1.2rem;
            border-radius: 4px;
            font-size: 1rem;
            cursor: pointer;
            text-decoration: none;
            transition: background 0.2s;
            margin-top: 0.5rem;
            text-align: center;
        }
        .add-to-cart-btn:hover {
            background: #0056b3;
        }
    </style>
</head>
<body>
    <h1>Welcome to the Home Page</h1>
    <div style="text-align:center; margin-bottom:2rem;"><%= name %></div>
    <div class="products-container">
        <% products.forEach(product => { %>
            <div class="product-card">
                <img src="https://fastly.picsum.photos/id/428/300/200.jpg?hmac=ikKOcamKDMicSZKD7eMbhzgMNNbyCucuLohsjaMt740" alt="<%= product.name %>">
                <div class="product-name"><%= product.name %></div>
                <div class="product-description"><%= product.description %></div>
                <div class="product-price">₹<%= product.price %></div>
                <a href="/user/product/<%= product._id %>" class="add-to-cart-btn">Add to Cart</a>
            </div>
        <% }) %>
    </div>
</body>
</html>